const name="Bharat";
const repoCount=0;
console.log(name+repoCount+"Raj");
//but the above way of concatanting strings is outdated , modern practice is to use backtick.

console.log(`Hello my name is ${name} and my repo count is${repoCount} and lastname is${" Raj"} `);
const str= new String("Bnarat");
// console.log(str);
// console.table(str);
console.log(str[3]);
console.log(str.toUpperCase());
console.log(str.substring(0,4/*which is excluded */));